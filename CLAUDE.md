# Reglas del proyecto (definidas por Jesús Olguín)

Estas reglas son **obligatorias** y aplican a este repo y a cualquier app, repo
o proyecto de Jesús (`jesus1942`).

## 1. Ramas
- **NUNCA** crear ramas sin el consentimiento explícito de Jesús.
- Trabajar directo sobre la rama principal (`main` / `master` según el repo),
  salvo que Jesús pida explícitamente una rama.
- Antes de crear cualquier rama, preguntar y esperar confirmación.

## 2. Autoría de commits
- **TODOS** los commits deben hacerse con el usuario de Jesús:
  - `user.name  = Jesus Olguin`
  - `user.email = chucky9425@gmail.com`
- No usar autores genéricos (p. ej. `Claude <noreply@anthropic.com>`).
- Verificar `git config user.name` / `user.email` antes de commitear.
